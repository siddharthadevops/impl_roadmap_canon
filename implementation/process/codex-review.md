# Codex Review Runner

This repository follows the common review canon:

[`../../canon/process/codex-review.md`](../../canon/process/codex-review.md)

## Local Deviations

- S2 (`agent99-tutor-process-consolidation`) uses an operator-authorized
  temporary broad/bypass reviewer invocation while it reviews the common
  invocation rule. The active milestone records the scope, no-edit prompt
  requirement, relevance-driven access boundary, and worktree snapshot
  requirement. The deviation expires when Slice 03 seals the common broad
  invocation rule or the milestone records a different explicit disposition.
  No repo-wide permanent deviation is active.
